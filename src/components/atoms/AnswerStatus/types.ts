export type QuestionStatusProps = {
  className?: string;
  children?: React.ReactNode;
  answerStatus: string;
};

export type BaseQuestionStatusProps = {
  statusColor: string;
  statusBackgroundColor: string;
};
